# Disabled in this fork.
# Upstream's version of this workflow is irrelevant to Altitude-Control-Technology/documenso
# (issue management / translations / community automation / upstream-only deploy).
# Future upstream changes to this file are auto-resolved via merge=ours in .gitattributes.
name: Disabled
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled in the fork."
